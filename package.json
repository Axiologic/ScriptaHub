{
  "name": "scriptahub-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:auth && npm run build:books",
    "build:auth": "node tools/build_auth.mjs",
    "build:books": "python3 -B tools/build_books.py refresh",
    "test": "npm run test:auth && npm run test:catalogue",
    "test:auth": "node --test tests/auth*.test.mjs",
    "test:catalogue": "python3 -B -m unittest discover -s tests -p test_catalogue_tools.py && python3 -B -m unittest discover -s tests -p test_internal_links.py && python3 -B tools/build_books.py check && python3 -B tools/audit_internal_links.py --check"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "openid-client": "6.8.7"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
