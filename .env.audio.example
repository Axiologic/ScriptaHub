# Optional cloud narration configuration. Copy into .env.audio and load with:
# --credentials-file .env.audio
# With no provider configured, audio.config.json selects local Piper.
# AUDIO_ENGINE=gemini
# GEMINI_API_KEY=
# OPENAI_API_KEY=
